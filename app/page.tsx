import { Button } from "@/components/ui/button";
import { Card, CardContent, CardDescription, CardHeader, CardTitle } from "@/components/ui/card";

export default function Home() {
  return (
    <main className="flex items-center justify-center h-[calc(100dvh)] w-full">
      <Card>
        <CardHeader>
          <CardTitle>Выйди и войди нармально</CardTitle>
          <CardDescription>чел ну ты это</CardDescription>
        </CardHeader>
        <CardContent>мда треш пон кнопок нет и чё а ты не парься ливни отсюда</CardContent>
      </Card>
    </main>
  )
}
